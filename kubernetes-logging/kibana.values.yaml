replicas: 3
tolerations:
- key: node-role
  operator: Equal
  value: infra
  effect: NoSchedule
nodeSelector:
  yandex.cloud/node-group-id: cathokilo1gk9f6t15bq
resources:
  requests:
    cpu: "500m"
    memory: "1Gi"
  limits:
    cpu: "500m"
    memory: "1Gi"
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: kibana.51.250.0.51.nip.io
      paths:
        - path: /
  # tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
